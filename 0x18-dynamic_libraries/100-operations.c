/**
 * add - calculates addition
 * @a: number
 * @b: number
 * Return: returns result
 */
int add(int a, int b)
{
	return (a + b);
}
/**
 * sub -calculates substration
 * @a: number
 * @b: number
 * Return: returns result
 */
int sub(int a, int b)
{
	return (a - b);
}
/**
 * mul - calculates multiplication
 * @a: number
 * @b: number
 * Return: return result
 */
int mul(int a, int b)
{
	return (a * b);
}
/**
 * div - calculates division
 * @a: number
 * @b: number
 * Return: returns  result
 */
int div(int a, int b)
{
	return (a / b);
}
/**
 * mod - calculates mod
 * @a: number
 * @b: nuber
 * Return: returns result
 */
int mod(int a, int b)
{
	return (a % b);
}
